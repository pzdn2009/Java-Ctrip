# Exchange

An Exchange is the message container holding the information during the entire routing of a {@link Message} received by a {@link Consumer}.

消息容器。包含了In-Message,Out-Message,元数据。

* MEP：MessageExchangePattern
* Property。元数据属性
* Message。In，Out。
* Exception。异常。
* Unitofwork。

